import { User } from "./user-model";

export class Student {
  userId!: number;
  user!: User;
  academicInfo!: string;
  resumeUrl!: string;
  skills!: string;
  careerObjectives!: string;
  internshipHistory!: string;
  feedbackAndRatings!: string;
}
